package stacksandqueues;

import static org.junit.Assert.*;

public class StackTest {

    //push on to stack
    //multiple nodes onto a stack
    //pop off the stack
    //successfully empty a stack via pops
    // instantiate an empty stack

}